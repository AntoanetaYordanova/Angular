import { HighlightDemoDirective } from './highlight-demo.directive';

describe('HighlightDemoDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightDemoDirective();
    expect(directive).toBeTruthy();
  });
});
